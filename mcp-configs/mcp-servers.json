{
  "mcpServers": {
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "YOUR_GITHUB_PAT_HERE"
      },
      "description": "GitHub operations - PRs, issues, repos"
    },
    "firecrawl": {
      "command": "npx",
      "args": ["-y", "firecrawl-mcp"],
      "env": {
        "FIRECRAWL_API_KEY": "YOUR_FIRECRAWL_KEY_HERE"
      },
      "description": "Web scraping and crawling"
    },
    "vercel": {
      "type": "http",
      "url": "https://mcp.vercel.com",
      "description": "Vercel deployments and projects"
    },
    "railway": {
      "command": "npx",
      "args": ["-y", "@railway/mcp-server"],
      "description": "Railway deployments"
    },
    "cloudflare-docs": {
      "type": "http",
      "url": "https://docs.mcp.cloudflare.com/mcp",
      "description": "Cloudflare documentation search"
    },
    "exa-web-search": {
      "command": "npx",
      "args": ["-y", "exa-mcp-server"],
      "env": {
        "EXA_API_KEY": "YOUR_EXA_API_KEY_HERE"
      },
      "description": "Web search and research via Exa API"
    },
    "playwright": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp", "--browser", "chrome"],
      "description": "Browser automation and testing via Playwright"
    },
    "vector-memory": {
      "command": "node",
      "args": ["/home/nod/.claude/vector-memory/server.js"],
      "description": "Local semantic memory for development patterns (TF-IDF search)"
    }
  },
  "_comments": {
    "removed": "supabase (no project-ref), memory (not needed with claude memory), sequential-thinking (built-in), cloudflare-workers-builds/bindings/observability (rarely used - re-add when needed), clickhouse (no config), context7 (provided by plugin), magic (rarely used), filesystem (claude has Read/Write), insaits (not configured), fal-ai (no key), browserbase (no key), browser-use (no key), devfleet (localhost only), token-optimizer (not needed), confluence (no config)"
  }
}
